# Security Policy

## 版本支持

当前仅维护 **最新主分支**。我们会及时修复影响安全性/私密性的问题，并在发布说明中标记。

## 报告安全问题

**请勿在公开 Issue 中报告安全漏洞。** 建议使用 GitHub 的**私密报告**功能
（仓库主页面 → Security → Advisories → Report a vulnerability），或在无法
使用该功能时通过维护者的邮箱私下联系。

请在报告中包含：

- 问题类型与受影响的组件（主机插件 `phone-lens` / 手机 App）
- 复现所需的环境与步骤（尽可能最小化）
- 影响范围（如：是否可越权访问、是否可伪造、是否可注入等）
- 你希望的联系方式

我们会在收到后尽快评估并回复；在修复发布前，我们不公开细节。

## 安全设计要点

- **回环免鉴权**：`127.0.0.1 / ::1` 可访问 `/ws/view`、`/view.html`、`/qr*`、`/upload`
- **非回环强鉴权**：局域网/USB 来源必须携带配对签发的设备 token，token 只存 SHA-256
- **配对码一次性**：8 位数字、TTL 15 分钟、使用即焚
- **预览不外泄**：`/ws/view`、`/view.html`、`/qr*` 对非回环来源直接 403
- **上传白名单**：`image/jpeg` / `image/png`，双验 Content-Type + magic bytes，默认 ≤10MB，并按来源限流
- **预览帧大小**：单帧 ≤ 512KB，避免内存滥用

## 使用建议

- 仅在**可信局域网**使用；不要把接收端口 8791 暴露到公网
- 保持 dsh 与 Flutter SDK 更新，使用最新依赖修复
