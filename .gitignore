# ── dependencies ────────────────────────────────────────────────
node_modules/
.pnpm-store/
.pub-cache/

# ── build output ────────────────────────────────────────────────
build/
dist/
*.apk
*.aab
*.ipa
*.jar

# ── dart / flutter ───────────────────────────────────────────────
.dart_tool/
.flutter-plugins-dependencies
.metadata
.packages
*.iml
.idea/

# ── gradle / android local ───────────────────────────────────────
.gradle/
android/.gradle/
android/local.properties

# ── editors / os ─────────────────────────────────────────────────
.vscode/
.DS_Store
Thumbs.db
*.log

# ── secrets / signing ────────────────────────────────────────────
*.jks
*.keystore
*.p12
key.properties

# ── local dev data ───────────────────────────────────────────────
.smoke-data*/
devices.json

# ── host plugin build artifacts (lib/client.js is a hand-written SOURCE,
#     keep it; the rest are regenerated by `pnpm build`) ─────────────
packages/lens-mate/lib/index.js
packages/lens-mate/lib/dev.js
packages/lens-mate/lib/*.map
packages/lens-mate/lib/index.d.ts
