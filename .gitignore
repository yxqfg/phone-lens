# ── dependencies ────────────────────────────────────────────────
node_modules/
.pnpm-store/
.pub-cache/

# ── build output ────────────────────────────────────────────────
build/
dist/
*.apk
*.aab
*.ipa
*.jar
*.tgz

# ── dart / flutter ───────────────────────────────────────────────
.dart_tool/
.flutter-plugins-dependencies
.metadata
.packages
*.iml
.idea/

# ── gradle / android local ───────────────────────────────────────
.gradle/
android/.gradle/
android/local.properties

# ── editors / os ─────────────────────────────────────────────────
.vscode/
.DS_Store
Thumbs.db
*.log

# ── secrets / signing ────────────────────────────────────────────
*.jks
*.keystore
*.p12
key.properties

# ── local dev data ───────────────────────────────────────────────
.smoke-data*/
devices.json

# ── host plugin: prebuilt artifacts ARE committed so that installs from
#     source (dshmarket / dsh plugin add <repo>) need no build step;
#     regenerate with `pnpm build`. Only source maps stay out. ─────────
packages/phone-lens/lib/*.map
